{
  "name": "holiday-poster-frontend",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "vite preview --host --port ${PORT:-4173}",
    "dev:all": "npm run dev:api --workspace=server & npm run dev",
    "dev:api": "npm --workspace server run dev"
  },
  "workspaces": [
    "server"
  ],
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.3.0"
  }
}
